# Phase 1 Backlog

This sheet keeps deliverables from `roadmap/PHASE-1-DATA-NETWORK.md` visible and ties them to code artifacts.

| Task | Owner | Status | Notes |
| --- | --- | --- | --- |
| Define vendor/product ingestion contracts, deduplication heuristics, and discovery connectors (LinkedIn, IndiaMART, Google Maps) plus the /api/v1/ingestion/discovery API | Platform Team | Completed | `backend/app/routers/ingestion.py`, `backend/app/services/discovery.py`, and connectors provide the contracts; discovery API triggers normalized data. |
| Build mock data loader for vendor/client enrichment plus marketing intent signals | Platform Team | Completed | `backend/scripts/seed_data.py` seeds vendors/products while respecting dedup and audit logs; marketing intent signals are now persisted on leads via `POST /api/v1/marketing/intent/event` and refreshed by `refresh_marketing_intent`. |
| Implement CRM relationship status tracking and customer onboarding APIs | Platform Team | Completed | `backend/app/routers/crm.py`, `backend/app/crm_models.py`, and `backend/app/schemas.py` cover leads, customers, consent, dashboard aggregation, communication tracking (`POST/GET /api/v1/crm/communications`), follow-up reminder automation (`send_follow_up_reminders`), sales funnel analytics (`GET /api/v1/crm/funnel`, `GET /api/v1/leads/{lead_id}/transitions`), automated sales playbooks (`GET /api/v1/leads/{lead_id}/playbook`, `GET /api/v1/crm/playbook-queue`), marketing analytics overview (`GET /api/v1/marketing/analytics`), campaign ROI overview (`GET /api/v1/marketing/roi`), nurture/re-engagement trigger API (`POST /api/v1/marketing/campaigns/trigger`), marketing automation provider dispatch API (`POST /api/v1/marketing/campaigns/dispatch`), and upsell/cross-sell trigger-dispatch API (`POST /api/v1/marketing/campaigns/upsell-cross-sell/trigger`). |
| Create Celery workflows for periodic enrichment/quality metrics | Platform Team | Completed | `backend/app/tasks.py` schedules `refresh_vendor_metadata`, `enrich_vendor_profiles`, `enrich_product_attributes_task`, `refresh_marketing_intent`, `compute_data_quality_metrics`, and monitoring jobs. |
| Establish baseline consent/opt-out tracking tables (schema + sample data) | Platform Team | Completed | Consent fields are present on `Lead` and `Customer` models, with APIs for lead preferences/unsubscribe and customer consent updates. |
| Document Phase 1 acceptance criteria and compliance checklist | Platform Team | Completed | `PHASE-1-IMPLEMENTATION-PLAN.md` and compliance endpoints (`/api/v1/compliance/report`, `/api/v1/compliance/scraping-controls`) provide traceable acceptance coverage, with locale-aware responses extended across compliance/operations/integrations/marketing APIs for Phase 1 multilingual scope. |
| Surface compliance reporting and governance summaries from AuditLog entries | Platform Team | Completed | backend/app/services/compliance.py and backend/app/routers/compliance.py power /api/v1/compliance/report; backend/tests/test_compliance.py validates the report payloads. |
| Add Docker Compose for local development (Postgres, Redis, backend, worker) | Platform Team | Completed | `infrastructure/docker-compose.yml`, `backend/Dockerfile`, and `.env` orchestrate the stack (including Celery beat). |
| Document enrichment path and monitoring workflow | Platform Team | Completed | project-management/ENRICHMENT-OVERVIEW.md outlines the flow and next steps. |
| Sketch admin categorization and override UX requirements | Platform Team | Completed | frontend/ADMIN-CATEGORIZATION-PLAN.md maps the screens, data flows, and backend hooks. |
| Expand ingestion/dedup/enrichment tests to cover the backend workflows | Platform Team | Completed | backend/tests/test_services.py and backend/tests/test_crud_enrichment.py now exercise dedup and enrichment logic. |
| Build monitoring dashboard for connector health and enrichment status | Platform Team | Completed | backend/app/routers/monitoring.py exposes /api/v1/monitoring/dashboard, backend/app/services/monitoring.py raises alerts, and backend/tests/test_monitoring.py covers monitoring snapshots. |
| Create Alembic migrations for the Phase 1 schema | Platform Team | Completed | `backend/alembic` includes env/template plus migrations `0001` to `0007` for schema, categorization/alerts, lead intelligence enrichment, CRM communication tracking, lead stage transition history, and marketing intent fields. |
